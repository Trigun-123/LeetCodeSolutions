    int garbageCollection(vector<string>& garbage, vector<int>& travel) {
        int n = garbage.size(), m = travel.size();
        int sum = 0, count = 0; 
        unordered_map<char, int> mpp;

        for(i = 0; i < n; i++) {
            count += garbage[i].size();

            for(auto iage[i]) {
                lait] = sum;
            }    
            if(i < m) {
                sum += travel[i];
            }    
        }

        return count + mpp['M'] + mpp['P'] + l['G'];
    }